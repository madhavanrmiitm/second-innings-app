import 'package:flutter/material.dart';

class EditTaskForSeniorPage extends StatelessWidget {
  const EditTaskForSeniorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Edit Task')),
      body: const Center(child: Text('Edit Task Page Placeholder')),
    );
  }
}
