import 'package:flutter/material.dart';

class EditReminderForSeniorPage extends StatelessWidget {
  const EditReminderForSeniorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Edit Reminder')),
      body: const Center(child: Text('Edit Reminder Page Placeholder')),
    );
  }
}
