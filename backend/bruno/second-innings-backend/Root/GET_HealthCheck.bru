meta {
  name: Get Health Check
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/
  body: none
  auth: none
}

docs {
  # Health Check

  Returns welcome message to confirm server is running.

  **Response:**
  ```json
  {
    "message": "Welcome to the FastAPI application"
  }
  ```
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
